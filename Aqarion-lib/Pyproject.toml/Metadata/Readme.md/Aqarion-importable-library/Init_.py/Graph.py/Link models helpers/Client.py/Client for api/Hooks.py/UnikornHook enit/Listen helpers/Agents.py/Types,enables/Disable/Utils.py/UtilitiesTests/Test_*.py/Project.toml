[project]
name = "aqarion"
version = "0.1.0"
description = "AQARIONZ graph + API client + hooks"
requires-python = ">=3.11"
dependencies = ["requests>=2.31"]

[build-system]
requires = ["setuptools>=65"]
build-backend = "setuptools.build_meta"
